frontendVersion: v1
kind: Service
metadata:
  name: grocerylist-frontend-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: grocerylist-frontend-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: grocerylist-frontend-deployment