apiVersion: v1
kind: Service
metadata:
  name: grocerylist-api-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: grocerylist-api-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: grocerylist-api-deployment